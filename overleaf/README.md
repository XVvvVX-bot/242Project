# Overleaf Upload Instructions

Upload the contents of this folder, or upload `retail_demand_forecasting_overleaf.zip`, to Overleaf.

Main file:

```text
main.tex
```

Required figure files are in:

```text
figures/
```

The project compiles with standard pdfLaTeX-compatible packages.
